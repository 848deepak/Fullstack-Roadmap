# TypeScript with React (Starter)

## Why TypeScript
- Better IDE support
- Safer refactoring
- Early error detection

## Starter steps
1. Create app with Vite React TS template
2. Type props and state first
3. Add API response interfaces
4. Use strict mode in `tsconfig`
