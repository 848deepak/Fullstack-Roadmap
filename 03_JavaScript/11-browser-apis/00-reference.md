# 00 — Browser APIs Quick Reference

## Core APIs
- `localStorage`, `sessionStorage`
- `navigator.geolocation`
- `IntersectionObserver`
- `navigator.clipboard`

## When to Use
- Persistence, viewport detection, user-location features, copy interactions.

## Safety Notes
- Handle permission denial (geolocation/clipboard).
- Avoid storing sensitive tokens in localStorage.
